function loadreport() {
    return alert("Loading weather report");
}

function hide(element) {
    console.log(element);
    console.log(element.parentNode);
    return element.parentNode.style.display = "none";
}

function ctof() {

    document.getElementsByClassName("temp")
    // iterate through the array
    for (var i = "24"; i < "75"; i++)
        if array[i].innerText == "24°"{
            // return array[i].innerText = "75°";
            // }else if{
            array[i].innerText = "24°"
        }
}